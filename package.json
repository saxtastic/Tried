{
  "name": "ayeyoty-co",
  "private": true,
  "version": "0.1.0",
  "description": "ayeyoty.co static site hosted on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "record-book:validate": "node record-book/scripts/validate.mjs",
    "record-book:build": "node record-book/scripts/build.mjs",
    "record-book:fetch": "node record-book/scripts/fetch-archives.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
